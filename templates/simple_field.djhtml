<div class="form-field">
    <strong>{{ field.label_tag }}</strong>

    {% if field.errors %}
    <div class="errors">{{ field.errors }}</div>
    {% endif %}

    {{ field }}

    {% if field.help_text %}
    <p>{{ field.help_text }}</p>
    {% endif %}
</div>
